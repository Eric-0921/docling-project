# :SYSTem:COMMunicate:NETWork[:IPADdress]:DNS <DNS>
Determines the net DNS server to resolve the name.

Parameters:

<DNS>

string

Example:

SYST:COMM:NETW:IPAD:DNS 123.456.0.1

Manual operation:

See "DNS Server" on page 107

