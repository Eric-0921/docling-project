# Knowledge Base Directory Structure

## Production (生产级知识库)

包含经过处理、优化的最终版本知识库，可直接用于 AI 检索和代码生成。

### 知识库列表

- **`production/smb100a/`** - SMB100A 信号发生器知识库
  - 来源：SMB100A Operating Manual（使用 Docling 转换）
  - 结构：`chapters/` + `index.json`
  - 状态：✅ 完成

- **`production/rsinstrument/`** - RsInstrument Python 库知识库
  - 来源：RsInstrument 官方文档（爬取 + 重组）
  - 结构：`chapters/` + `index.json`
  - 状态：✅ 完成

- **`production/oe1022d/`** - OE1022D 锁相放大器知识库
  - 来源：OE1022D Manual（使用 Docling 转换）
  - 状态：🚧 待完成

## Archive (归档)

包含中间过程文件、实验数据和原始转换输出，仅用于参考或调试。

### 归档内容

- **`archive/smb100a_experiments/`** - SMB100A 实验和中间文件
  - `demo_kb/`, `demo_scpi/` - 早期实验
  - `run_xxx/` - 各次转换运行记录
  - `split_chapters/` - 章节拆分实验

- **`archive/rsinstrument_raw/`** - RsInstrument 原始爬取数据
  - 包含未经处理的大文件版本

- **`archive/oe1022d_runs/`** - OE1022D 转换运行记录
  - 包含各次转换尝试的输出

## 使用指南

### 对于 AI 应用开发
直接使用 `production/` 目录下的知识库：
```python
# 示例：加载 SMB100A 知识库
import json
with open('knowledge_base/production/smb100a/index.json') as f:
    kb_index = json.load(f)
```

### 对于调试或研究
参考 `archive/` 目录中的中间文件，了解转换流程或问题排查。

## 目录结构

```
knowledge_base/
├── production/              # 生产级知识库
│   ├── smb100a/
│   │   ├── chapters/        # 1500+ 个章节文件
│   │   └── index.json       # 结构化索引
│   ├── rsinstrument/
│   │   ├── chapters/        # 34 个章节文件
│   │   ├── _images/         # 图片资源
│   │   └── index.json
│   └── oe1022d/             # (待添加)
│       └── ...
└── archive/                 # 归档
    ├── smb100a_experiments/
    ├── rsinstrument_raw/
    └── oe1022d_runs/
```

## 维护说明

- **添加新知识库**：在 `production/` 下创建新目录，遵循 `chapters/` + `index.json` 结构
- **归档规则**：所有中间文件、实验数据存放在 `archive/`
- **命名规范**：生产级知识库使用简洁名称（如 `smb100a`），归档使用描述性名称（如 `smb100a_experiments`）
