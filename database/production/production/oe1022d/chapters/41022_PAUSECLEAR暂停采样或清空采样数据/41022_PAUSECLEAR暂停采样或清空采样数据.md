## 4.10.2.2 [PAUSE/CLEAR]：暂停采样或清空采样数据

[PAUSE/CLEAR] 按键功能有两个，一是暂停采样 [PAUSE], 二是复位采样 [CLEAR] 。

图会跟 图

，二是在其他状态下重

数据采样进行中可通过该按键来暂停数据采样，如果在 <Chart> 图状态， <Chart> 着暂停。在暂停状态下再次按一次该按键会清空采样数据并转为停止状态。如果 <Chart> 状态，所绘制的采样曲线也会同时清除。
