# :SYSTem:COMMunicate:NETWork[:IPADdress]:SUBNet:MASK <Mask>
Sets the subnet mask.

Parameters:

<Mask>

string

Example:

SYSTem:COMMunicate:NETWork:IPADdress:SUBNet:

MASK '255.255.0.0'

determines the subnet mask.

Manual operation:

See "Subnet Mask" on page 106

